vagrant-docker-td-es-kibana-sample
==================================

vagrant, docker, td-agent, elasticsearch, kibana

# Usage

    $ vagrant up
    $ open localhost:5555
    $ cat '{"data": 1}' | fluent-cat
